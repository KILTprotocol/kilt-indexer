{
  "name": "kilt-indexer",
  "version": "0.0.1",
  "description": "indexing of attestations and CTypes",
  "main": "dist/index.js",
  "packageManager": "yarn@4.3.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "codegen": "subql codegen",
    "build": "subql build",
    "start:docker": "docker compose pull && docker compose up --remove-orphans",
    "dev": "subql codegen && subql build && docker compose pull && docker compose up --build --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "subql build && subql-node test",
    "slash": "yarn ts-node src/slash.ts"
  },
  "homepage": "https://github.com/KILTprotocol/attestation-indexer",
  "repository": "github:KILTprotocol/attestation-indexer",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Botlabs GmbH",
  "license": "MIT",
  "dependencies": {
    "@kiltprotocol/sdk-js": "^0.35.0",
    "@polkadot/api": "^10.13.1",
    "dotenv": "^16.4.5",
    "jsonabc": "^2.3.1"
  },
  "devDependencies": {
    "@apollo/client": "^3.9.11",
    "@kiltprotocol/augment-api": "^0.35.0",
    "@kiltprotocol/type-definitions": "^0.35.0",
    "@kiltprotocol/types": "^0.35.0",
    "@subql/cli": "^4.5.2",
    "@subql/node": "^4.1.0",
    "@subql/testing": "^2.1.0",
    "@subql/types": "^3.4.0",
    "@types/jsonabc": "^2.3.3",
    "@types/node": "^20.12.7",
    "graphql": "^16.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  },
  "exports": {
    "chaintypes": "./src/chaintypes.ts"
  }
}
